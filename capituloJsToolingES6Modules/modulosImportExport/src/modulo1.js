const nome = 'Marcelo';
const sobrenome = 'Pereira dos Santos';
const idade = 41;

function soma(x, y) {
  return x + y;
}

export default (x, y) => x * y;

export {
  nome, sobrenome, idade, soma,
};
